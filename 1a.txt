#include <stdio.h>
#include <fcntl.h>
#include <unistd.h>

int main(int argc, char *argv[]) {
    int fd;
    char ch;
    off_t size;

    if (argc < 2) {
        printf("Usage: %s <filename>\n", argv[0]);
        return 1;
    }

    fd = open(argv[1], O_RDONLY);
    if (fd < 0) {
        perror("open");
        return 1;
    }

    // Find file size
    size = lseek(fd, 0, SEEK_END);
    if (size < 0) {
        perror("lseek");
        close(fd);
        return 1;
    }

    printf("File content in reverse order:\n");

    // Read backwards
    while (size > 0) {
        size--;
        lseek(fd, size, SEEK_SET);
        read(fd, &ch, 1);
        write(STDOUT_FILENO, &ch, 1);
    }
    printf("\n");

    close(fd);
    return 0;
}

/*
HOW TO RUN:
vi 1a.c        (paste the above code, then :wq to save)
cc 1a.c
echo "Hello World" > testfile.txt
./a.out testfile.txt
*/
